name: LittleHabitTracker
options:
  bundleIdPrefix: com.xiaodao
  deploymentTarget:
    iOS: 26.0
targets:
  LittleHabitTracker:
    type: application
    platform: iOS
    sources:
      - path: Sources
    info:
      path: Sources/Info.plist
      properties:
        UILaunchScreen: {}
        UIBackgroundModes:
          - remote-notification
